using System;
namespace SeleDreams.DataManagement.BattleData
{
    public enum BattleType
    {
        INVALID,
        BINARY,
        ASM,
        C,
        CPP,
        CS,
        VB,
        JS,
        HTML,
        CSS
    }
}
